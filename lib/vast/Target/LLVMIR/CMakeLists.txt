get_property(VAST_DIALECT_LIBS GLOBAL PROPERTY VAST_DIALECT_LIBS)
get_property(VAST_CONVERSION_LIBS GLOBAL PROPERTY VAST_CONVERSION_LIBS)

add_vast_conversion_library(TargetLLVMIR
    Convert.cpp

    LINK_LIBS
    ${MLIR_LIBS}
    ${VAST_DIALECT_LIBS}
    ${VAST_CONVERSION_LIBS}
)
